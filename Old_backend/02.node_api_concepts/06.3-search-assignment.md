# Assignment 03

**Course:** Backend with Node.js
**Topics:** REST API · CRUD · Search APIs · Combined Queries · MVC   **Deadline:** 27th April

---

## Goal

This is the continuation of Assignment 02. In Assignment you already implemented CRUD operations and learned route params, query params, pagination, and sorting.

Now, you will build this assignment **from scratch again in a new project**, and extend it by adding **search functionality** and combining multiple concepts together.

You will:
- Rebuild all previous concepts (CRUD, params, pagination, sorting)  
- Implement search using MongoDB `$regex`  
- Combine search with filtering, sorting, and pagination  
- Build advanced endpoints that handle multiple query operations at once  
- Finally, create a **master query endpoint** that supports everything in a single API call  

---

### Important Instruction

-  Do NOT use your Assignment 02 project  
-  Do NOT copy-paste code from previous assignments  
-  Do NOT continue in the same folder or repository  
-  You must create a completely **new project from scratch**  
-  Re-implement everything step by step  

---

###  Why This Matters

- Ensures you truly understand all concepts (not just reuse code)  
- Builds confidence to create APIs from scratch  
- Prepares you for real interview and job scenarios  

---

###  Final Objective

By the end of this assignment, you should be able to:

- Build a complete backend project independently  
- Handle complex query logic cleanly  
- Design production-level APIs with search, filter, sort, and pagination  

---

This assignment is focused on building **real-world query APIs from scratch**, not extending old code.

---

##  Mandatory Development Discipline

### Git Commit Rule (Strictly Required)

After completing **each endpoint**, you must create a commit.

**Do NOT implement all endpoints and commit once. This is not allowed.**

---

###  Required Workflow

1. Implement one endpoint (e.g., `POST /api/notes`)
2. Test it properly using Postman
3. Ensure:
   - Correct functionality
   - Proper status codes
   - Correct response format
4. Commit immediately after verification

---

### Commit Message Format

Use clean and professional commit messages:

```bash
git add .
git commit -m "feat: add create note endpoint (POST /api/notes)"

```
---
###  Evaluation Impact (Strict)

Failure to follow any of the above rules will result in:

-  Direct Assignment Rejection  
-  No evaluation will be done  


---

## Same Folder Structure

```
notes-app/
│
├── src/
│   ├── config/
│   │   └── db.js
│   ├── models/
│   │   └── note.model.js
│   ├── controllers/
│   │   └── note.controller.js
│   ├── routes/
│   │   └── note.routes.js
│   ├── middlewares/
│   ├── app.js
│   └── index.js
│
├── .env
├── .env.example
└── package.json
```

---

## Mongoose Schema (unchanged)

```js
const noteSchema = new mongoose.Schema(
  {
    title:    { type: String, required: [true, "Title is required"] },
    content:  { type: String, required: [true, "Content is required"] },
    category: { type: String, enum: ["work", "personal", "study"], default: "personal" },
    isPinned: { type: Boolean, default: false },
  },
  { timestamps: true }
);
```

---

## All Endpoints — This File

| # | Method | Endpoint | Concepts |
|---|--------|----------|----------|
| **CRUD** | | | |
| 1 | `POST` | `/api/notes` | Create single |
| 2 | `POST` | `/api/notes/bulk` | Create bulk |
| 3 | `GET` | `/api/notes` | Read all |
| 4 | `GET` | `/api/notes/:id` | Read by ID |
| 5 | `PUT` | `/api/notes/:id` | Full replace |
| 6 | `PATCH` | `/api/notes/:id` | Partial update |
| 7 | `DELETE` | `/api/notes/:id` | Delete single |
| 8 | `DELETE` | `/api/notes/bulk` | Delete bulk |
| **Search** | | | |
| 9 | `GET` | `/api/notes/search` | Search title only |
| 10 | `GET` | `/api/notes/search/content` | Search content only |
| 11 | `GET` | `/api/notes/search/all` | Search title + content |
| **Two Concepts Combined** | | | |
| 12 | `GET` | `/api/notes/filter-sort` | Query params + Sorting |
| 13 | `GET` | `/api/notes/filter-paginate` | Query params + Pagination |
| 14 | `GET` | `/api/notes/sort-paginate` | Sorting + Pagination |
| 15 | `GET` | `/api/notes/search-filter` | Search + Query params |
| **Three Concepts Combined** | | | |
| 16 | `GET` | `/api/notes/search-sort-paginate` | Search + Sort + Paginate |
| 17 | `GET` | `/api/notes/filter-sort-paginate` | Filter + Sort + Paginate |
| **Master Endpoint** | | | |
| 18 | `GET` | `/api/notes/query` | Everything — search + filter + sort + paginate |

---

## CRUD Endpoints

> All 8 must be working. Same as Assignment 01 and 02a.

---

### 1. POST `/api/notes` — Create a note

**Request body:**
```json
{
  "title": "Team standup agenda",
  "content": "Discuss sprint blockers and deployment plan",
  "category": "work",
  "isPinned": true
}
```

**Success — `201`:**
```json
{
  "success": true,
  "message": "Note created successfully",
  "data": {}
}
```

**Missing title or content — `400`:**
```json
{
  "success": false,
  "message": "Title and content are required",
  "data": null
}
```

---

### 2. POST `/api/notes/bulk` — Create multiple notes

**Request body:**
```json
{
  "notes": [
    { "title": "Note one",   "content": "Content one",   "category": "work"     },
    { "title": "Note two",   "content": "Content two",   "category": "study"    },
    { "title": "Note three", "content": "Content three", "category": "personal" }
  ]
}
```

**Success — `201`:**
```json
{
  "success": true,
  "message": "3 notes created successfully",
  "data": []
}
```

**Empty or missing notes array — `400`:**
```json
{
  "success": false,
  "message": "notes array is required and cannot be empty",
  "data": null
}
```

> Use `Note.insertMany()`.

---

### 3. GET `/api/notes` — Get all notes

**Success — `200`:**
```json
{
  "success": true,
  "message": "Notes fetched successfully",
  "count": 10,
  "data": []
}
```

---

### 4. GET `/api/notes/:id` — Get note by ID

**Success — `200`:**
```json
{
  "success": true,
  "message": "Note fetched successfully",
  "data": {}
}
```

**Invalid ObjectId — `400`:**
```json
{
  "success": false,
  "message": "Invalid note ID",
  "data": null
}
```

**Not found — `404`:**
```json
{
  "success": false,
  "message": "Note not found",
  "data": null
}
```

---

### 5. PUT `/api/notes/:id` — Full replace

PUT = **full replacement**. Fields not sent are reset to defaults.

**Request body:**
```json
{
  "title": "Completely new title",
  "content": "Completely new content",
  "category": "personal",
  "isPinned": false
}
```

**Success — `200`:**
```json
{
  "success": true,
  "message": "Note replaced successfully",
  "data": {}
}
```

> Use `{ new: true, overwrite: true, runValidators: true }`.

---

### 6. PATCH `/api/notes/:id` — Partial update

PATCH = **update only the fields you send**. Everything else stays.

**Request body:**
```json
{
  "isPinned": true
}
```

**Success — `200`:**
```json
{
  "success": true,
  "message": "Note updated successfully",
  "data": {}
}
```

**Empty body — `400`:**
```json
{
  "success": false,
  "message": "No fields provided to update",
  "data": null
}
```

> Use `{ new: true, runValidators: true }`.

---

### 7. DELETE `/api/notes/:id` — Delete single

**Success — `200`:**
```json
{
  "success": true,
  "message": "Note deleted successfully",
  "data": null
}
```

---

### 8. DELETE `/api/notes/bulk` — Delete multiple

**Request body:**
```json
{
  "ids": [
    "64b1f2c3e4d5a6b7c8d9e0f1",
    "64b1f2c3e4d5a6b7c8d9e0f2"
  ]
}
```

**Success — `200`:**
```json
{
  "success": true,
  "message": "2 notes deleted successfully",
  "data": null
}
```

**Empty ids array — `400`:**
```json
{
  "success": false,
  "message": "ids array is required and cannot be empty",
  "data": null
}
```

> Use `Note.deleteMany({ _id: { $in: ids } })`.

---

## Section 5 — Search

> **What is search?**
> Search uses MongoDB `$regex` to find documents where a field contains the keyword.
> The `i` option makes it case-insensitive — `"MEETING"` and `"meeting"` both match.
>
> Use `$or` when you want to search across multiple fields at once.

---

### 9. GET `/api/notes/search` — Search in title only

**Required param:**

| Param | Required | Description |
|-------|----------|-------------|
| `q` | Yes | Keyword to search for |

**Example requests:**
```
GET /api/notes/search?q=meeting
GET /api/notes/search?q=sprint
GET /api/notes/search?q=node
```

**Missing `q` — `400`:**
```json
{
  "success": false,
  "message": "Search query 'q' is required",
  "data": null
}
```

**Success — `200`:**
```json
{
  "success": true,
  "message": "Search results for: meeting",
  "count": 2,
  "data": []
}
```

> MongoDB regex on a single field:
```js
const notes = await Note.find({
  title: { $regex: req.query.q, $options: "i" }
});
```

---

### 10. GET `/api/notes/search/content` — Search in content only

Same as Endpoint 9 but searches inside the `content` field.

**Example requests:**
```
GET /api/notes/search/content?q=deployment
GET /api/notes/search/content?q=hooks
GET /api/notes/search/content?q=blockers
```

**Missing `q` — `400`:**
```json
{
  "success": false,
  "message": "Search query 'q' is required",
  "data": null
}
```

**Success — `200`:**
```json
{
  "success": true,
  "message": "Content search results for: deployment",
  "count": 1,
  "data": []
}
```

---

### 11. GET `/api/notes/search/all` — Search in title AND content

A note matches if the keyword appears in **either** `title` or `content`.

**Example requests:**
```
GET /api/notes/search/all?q=react
GET /api/notes/search/all?q=meeting
GET /api/notes/search/all?q=node
```

**Success — `200`:**
```json
{
  "success": true,
  "message": "Search results for: react",
  "count": 4,
  "data": []
}
```

> Use `$or` to match across both fields:
```js
const notes = await Note.find({
  $or: [
    { title:   { $regex: req.query.q, $options: "i" } },
    { content: { $regex: req.query.q, $options: "i" } }
  ]
});
```

---

## Section 6 — Two Concepts Combined

> Now you start combining. Each endpoint uses exactly two concepts at the same time. Think about how to merge the query objects before calling `.find()`.

---

### 12. GET `/api/notes/filter-sort` — Filter + Sort

Filter by category and/or isPinned — and sort the results.

**Supported params:**

| Param | Required | Description |
|-------|----------|-------------|
| `category` | No | Filter by category |
| `isPinned` | No | Filter by pinned status |
| `sortBy` | No | Field to sort (default: `createdAt`) |
| `order` | No | `asc` or `desc` (default: `desc`) |

**Example requests:**
```
GET /api/notes/filter-sort?category=work&sortBy=title&order=asc
GET /api/notes/filter-sort?isPinned=true&sortBy=createdAt&order=desc
GET /api/notes/filter-sort?category=study&isPinned=false&sortBy=title&order=asc
```

**Success — `200`:**
```json
{
  "success": true,
  "message": "Notes fetched successfully",
  "count": 3,
  "data": []
}
```

---

### 13. GET `/api/notes/filter-paginate` — Filter + Paginate

Filter by category and/or isPinned — and paginate the filtered results.

**Supported params:**

| Param | Required | Description |
|-------|----------|-------------|
| `category` | No | Filter by category |
| `isPinned` | No | Filter by pinned status |
| `page` | No | Page number (default: `1`) |
| `limit` | No | Results per page (default: `10`) |

**Example requests:**
```
GET /api/notes/filter-paginate?category=work&page=1&limit=3
GET /api/notes/filter-paginate?isPinned=true&page=1&limit=5
GET /api/notes/filter-paginate?category=personal&isPinned=false&page=2&limit=4
```

**Success — `200`:**
```json
{
  "success": true,
  "message": "Notes fetched successfully",
  "data": [],
  "pagination": {
    "total": 8,
    "page": 1,
    "limit": 3,
    "totalPages": 3,
    "hasNextPage": true,
    "hasPrevPage": false
  }
}
```

> `countDocuments` must use the same filter as `find` — otherwise your total count will be wrong:
```js
const filter = {};
if (category) filter.category = category;
if (isPinned !== undefined) filter.isPinned = isPinned === "true";

const total = await Note.countDocuments(filter);
const notes = await Note.find(filter).skip(skip).limit(limitNum);
```

---

### 14. GET `/api/notes/sort-paginate` — Sort + Paginate

Sort all notes — and paginate. No filtering here.

**Supported params:**

| Param | Required | Description |
|-------|----------|-------------|
| `sortBy` | No | Field to sort (default: `createdAt`) |
| `order` | No | `asc` or `desc` (default: `desc`) |
| `page` | No | Page number (default: `1`) |
| `limit` | No | Results per page (default: `10`) |

**Example requests:**
```
GET /api/notes/sort-paginate?sortBy=title&order=asc&page=1&limit=5
GET /api/notes/sort-paginate?sortBy=createdAt&order=desc&page=2&limit=4
```

**Success — `200`:**
```json
{
  "success": true,
  "message": "Notes fetched successfully",
  "data": [],
  "pagination": {
    "total": 15,
    "page": 1,
    "limit": 5,
    "totalPages": 3,
    "hasNextPage": true,
    "hasPrevPage": false
  }
}
```

---

### 15. GET `/api/notes/search-filter` — Search + Filter

Search by keyword — and filter by category and/or isPinned at the same time.

**Supported params:**

| Param | Required | Description |
|-------|----------|-------------|
| `q` | Yes | Search keyword |
| `category` | No | Filter by category |
| `isPinned` | No | Filter by pinned status |

**Example requests:**
```
GET /api/notes/search-filter?q=meeting&category=work
GET /api/notes/search-filter?q=react&isPinned=true
GET /api/notes/search-filter?q=sprint&category=work&isPinned=false
```

**Missing `q` — `400`:**
```json
{
  "success": false,
  "message": "Search query 'q' is required",
  "data": null
}
```

**Success — `200`:**
```json
{
  "success": true,
  "message": "Search results for: meeting",
  "count": 2,
  "data": []
}
```

> Merge the search `$or` and other filters into one object:
```js
const filter = {};

if (q) {
  filter.$or = [
    { title:   { $regex: q, $options: "i" } },
    { content: { $regex: q, $options: "i" } }
  ];
}
if (category) filter.category = category;
if (isPinned !== undefined) filter.isPinned = isPinned === "true";
```

---

## Section 7 — Three or More Concepts Combined

> These endpoints combine three or four concepts into one. This is what real production APIs look like. Build them carefully — get each piece working first, then combine.

---

### 16. GET `/api/notes/search-sort-paginate` — Search + Sort + Paginate

Search by keyword — sort the results — and paginate them. Three concepts at once.

**Supported params:**

| Param | Required | Default | Description |
|-------|----------|---------|-------------|
| `q` | Yes | — | Search keyword |
| `sortBy` | No | `createdAt` | Field to sort |
| `order` | No | `desc` | Sort direction |
| `page` | No | `1` | Page number |
| `limit` | No | `10` | Results per page |

**Example requests:**
```
GET /api/notes/search-sort-paginate?q=node&sortBy=title&order=asc&page=1&limit=5
GET /api/notes/search-sort-paginate?q=meeting&sortBy=createdAt&order=desc&page=1&limit=3
```

**Missing `q` — `400`:**
```json
{
  "success": false,
  "message": "Search query 'q' is required",
  "data": null
}
```

**Success — `200`:**
```json
{
  "success": true,
  "message": "Search results for: node",
  "data": [],
  "pagination": {
    "total": 6,
    "page": 1,
    "limit": 5,
    "totalPages": 2,
    "hasNextPage": true,
    "hasPrevPage": false
  }
}
```

---

### 17. GET `/api/notes/filter-sort-paginate` — Filter + Sort + Paginate

Filter by category and isPinned — sort — and paginate. Three concepts together.

**Supported params:**

| Param | Required | Default | Description |
|-------|----------|---------|-------------|
| `category` | No | — | Filter by category |
| `isPinned` | No | — | Filter by pinned status |
| `sortBy` | No | `createdAt` | Field to sort |
| `order` | No | `desc` | Sort direction |
| `page` | No | `1` | Page number |
| `limit` | No | `10` | Results per page |

**Example requests:**
```
GET /api/notes/filter-sort-paginate?category=work&sortBy=title&order=asc&page=1&limit=3
GET /api/notes/filter-sort-paginate?isPinned=true&sortBy=createdAt&order=desc&page=1&limit=5
GET /api/notes/filter-sort-paginate?category=study&isPinned=false&sortBy=title&order=asc&page=2&limit=4
```

**Success — `200`:**
```json
{
  "success": true,
  "message": "Notes fetched successfully",
  "data": [],
  "pagination": {
    "total": 10,
    "page": 1,
    "limit": 3,
    "totalPages": 4,
    "hasNextPage": true,
    "hasPrevPage": false
  }
}
```

---

## The Master Endpoint

### 18. GET `/api/notes/query` — Everything at once

This is the final endpoint. Every param is optional. When all are used together, this endpoint does search + filter + sort + paginate in a single call. This is what a real production query API looks like.

**Supported params:**

| Param | Required | Default | Description |
|-------|----------|---------|-------------|
| `q` | No | — | Search in title and content |
| `category` | No | — | Filter by category |
| `isPinned` | No | — | Filter by pinned status |
| `sortBy` | No | `createdAt` | Field to sort by |
| `order` | No | `desc` | Sort direction |
| `page` | No | `1` | Page number |
| `limit` | No | `10` | Results per page |

**Example requests — test every combination:**
```
GET /api/notes/query
GET /api/notes/query?q=react
GET /api/notes/query?category=work
GET /api/notes/query?isPinned=true&sortBy=title&order=asc
GET /api/notes/query?q=meeting&category=work&sortBy=createdAt&order=desc&page=1&limit=5
GET /api/notes/query?q=node&category=study&isPinned=false&sortBy=updatedAt&order=desc&page=2&limit=3
```

**Success — `200`:**
```json
{
  "success": true,
  "message": "Notes fetched successfully",
  "data": [],
  "pagination": {
    "total": 12,
    "page": 1,
    "limit": 5,
    "totalPages": 3,
    "hasNextPage": true,
    "hasPrevPage": false
  }
}
```

> **Full query builder — build it in this exact order:**
```js
const getAllNotesQuery = async (req, res) => {
  try {
    const { q, category, isPinned, sortBy, order, page, limit } = req.query;

    // Step 1 — Build filter
    const filter = {};

    if (q) {
      filter.$or = [
        { title:   { $regex: q, $options: "i" } },
        { content: { $regex: q, $options: "i" } }
      ];
    }
    if (category)                filter.category = category;
    if (isPinned !== undefined)  filter.isPinned  = isPinned === "true";

    // Step 2 — Sorting
    const allowedSortFields = ["title", "createdAt", "updatedAt", "category"];
    const sortField = allowedSortFields.includes(sortBy) ? sortBy : "createdAt";
    const sortOrder = order === "asc" ? 1 : -1;

    // Step 3 — Pagination
    const pageNum  = parseInt(page)  || 1;
    const limitNum = parseInt(limit) || 10;
    const skip     = (pageNum - 1) * limitNum;

    // Step 4 — Execute
    const total = await Note.countDocuments(filter);
    const notes = await Note.find(filter)
      .sort({ [sortField]: sortOrder })
      .skip(skip)
      .limit(limitNum);

    res.status(200).json({
      success: true,
      message: "Notes fetched successfully",
      data: notes,
      pagination: {
        total,
        page:        pageNum,
        limit:       limitNum,
        totalPages:  Math.ceil(total / limitNum),
        hasNextPage: pageNum < Math.ceil(total / limitNum),
        hasPrevPage: pageNum > 1,
      },
    });

  } catch (error) {
    res.status(500).json({ success: false, message: error.message, data: null });
  }
};
```

---

## Route Order in note.routes.js

```js
// ✅ CORRECT ORDER — add these BEFORE the /:id routes from 02a

// CRUD bulk
router.post(  "/bulk", createBulkNotes);
router.delete("/bulk", deleteBulkNotes);

// Search routes
router.get("/search/content",          searchByContent);
router.get("/search/all",              searchAll);
router.get("/search",                  searchByTitle);

// Combination routes
router.get("/filter-sort",             filterAndSort);
router.get("/filter-paginate",         filterAndPaginate);
router.get("/sort-paginate",           sortAndPaginate);
router.get("/search-filter",           searchAndFilter);
router.get("/search-sort-paginate",    searchSortPaginate);
router.get("/filter-sort-paginate",    filterSortPaginate);
router.get("/query",                   masterQuery);

// CRUD single-item routes LAST
router.post(  "/",         createNote);
router.get(   "/",         getAllNotes);
router.get(   "/:id/summary", getNoteSummary);
router.get(   "/:id",      getNoteById);
router.put(   "/:id",      replaceNote);
router.patch( "/:id",      updateNote);
router.delete("/:id",      deleteNote);
```

---

## Response Format

Every endpoint must follow this exact format:

```json
{
  "success": true,
  "message": "...",
  "data": [] or {} or null
}
```

List endpoints include `count`. Paginated endpoints include `pagination`.

---

## HTTP Status Codes

| Code | When |
|------|------|
| `200` | Successful GET, PUT, PATCH, DELETE |
| `201` | Successful POST |
| `400` | Missing required field, invalid param, empty body |
| `404` | Note not found |
| `500` | Server or database error |

---

## Environment Variables

**.env:**
```
MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/notes-db
PORT=5000
```

**.env.example:**
```
MONGO_URI=your_mongodb_connection_string_here
PORT=5000
```

---

## Packages

```bash
npm install express mongoose dotenv
npm install --save-dev nodemon
```

---

## Submission

| # | What to submit |
|---|----------------|
| 1 | **GitHub Repository Link** — public repo |
| 2 | **Postman Documentation** — published collection link |
| 3 | **Deployed Backend Link** — live API URL |

---
