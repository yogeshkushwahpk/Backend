# Assignment 01 

**Course:** Backend with Node.js 
**Topics:** REST API · CRUD · MongoDB · Mongoose · MVC
**Deadline:** 18th April


---

## Goal

Build a **Notes Management REST API** using **Node.js + Express + MongoDB (Mongoose)**.

First complete backend project. Focus on getting the fundamentals right — database connection, schema design, MVC structure, and all CRUD operations using the correct HTTP methods.

No frontend. No authentication. No extra features. Just clean REST.

---


##  Mandatory Development Discipline

### Git Commit Rule (Strictly Required)

After completing **each endpoint**, you must create a commit.

**Do NOT implement all endpoints and commit once. This is not allowed.**

---

###  Required Workflow

1. Implement one endpoint (e.g., `POST /api/notes`)
2. Test it properly using Postman
3. Ensure:
   - Correct functionality
   - Proper status codes
   - Correct response format
4. Commit immediately after verification

---

### Commit Message Format

Use clean and professional commit messages:

```bash
git add .
git commit -m "feat: add create note endpoint (POST /api/notes)"

```


---
###  Evaluation Impact (Strict)

Failure to follow any of the above rules will result in:

- ❌ Direct Assignment Rejection  
- ❌ No evaluation will be done  
---

## Folder Structure

```
notes-app/
│
├── src/
│   ├── config/
│   │   └── db.js
│   │
│   ├── models/
│   │   └── note.model.js
│   │
│   ├── controllers/
│   │   └── note.controller.js
│   │
│   ├── routes/
│   │   └── note.routes.js
│   │
│   ├── middlewares/               ← Create folder, leave empty for now
│   │
│   ├── app.js
│   └── index.js
│
├── .env
├── .env.example
└── package.json
```

| File | What it must contain |
|------|----------------------|
| `config/db.js` | `mongoose.connect()` only |
| `models/note.model.js` | Mongoose schema + model only |
| `controllers/note.controller.js` | All logic, DB queries, responses |
| `routes/note.routes.js` | Route definitions only — no logic |
| `app.js` | Express setup, `express.json()`, mount routes |
| `index.js` | `app.listen()` only |

---

## Mongoose Schema

File: `src/models/note.model.js`

```js
const noteSchema = new mongoose.Schema(
  {
    title:    { type: String, required: [true, "Title is required"] },
    content:  { type: String, required: [true, "Content is required"] },
    category: { type: String, enum: ["work", "personal", "study"], default: "personal" },
    isPinned: { type: Boolean, default: false },
  },
  { timestamps: true }
);
```

| Field | Type | Required | Details |
|-------|------|----------|---------|
| `title` | String | Yes | Cannot be empty |
| `content` | String | Yes | Cannot be empty |
| `category` | String | No | Enum: `work` / `personal` / `study` — Default: `personal` |
| `isPinned` | Boolean | No | Default: `false` |
| `createdAt` | Date | Auto | Auto-generated by `timestamps: true` |
| `updatedAt` | Date | Auto | Auto-generated by `timestamps: true` |

---

## API Endpoints

Base URL: `/api/notes`

| # | Method | Endpoint | Description |
|---|--------|----------|-------------|
| 1 | `POST` | `/api/notes` | Create a single note |
| 2 | `POST` | `/api/notes/bulk` | Create multiple notes at once |
| 3 | `GET` | `/api/notes` | Get all notes |
| 4 | `GET` | `/api/notes/:id` | Get a single note by ID |
| 5 | `PUT` | `/api/notes/:id` | Replace a note completely |
| 6 | `PATCH` | `/api/notes/:id` | Update specific fields only |
| 7 | `DELETE` | `/api/notes/:id` | Delete a single note |
| 8 | `DELETE` | `/api/notes/bulk` | Delete multiple notes by IDs |

---

## Endpoint Details

---

### 1. POST `/api/notes` — Create a note

**Request body:**
```json
{
  "title": "Team standup agenda",
  "content": "Discuss sprint blockers and deployment plan",
  "category": "work",
  "isPinned": true
}
```

**Success response — `201`:**
```json
{
  "success": true,
  "message": "Note created successfully",
  "data": { }
}
```

---

### 2. POST `/api/notes/bulk` — Create multiple notes

**Request body:**
```json
{
  "notes": [
    { "title": "Note one",   "content": "Content one",   "category": "work"     },
    { "title": "Note two",   "content": "Content two",   "category": "study"    },
    { "title": "Note three", "content": "Content three", "category": "personal" }
  ]
}
```

**Success response — `201`:**
```json
{
  "success": true,
  "message": "3 notes created successfully",
  "data": [ ]
}
```

> Use `Note.insertMany()` for this endpoint.

---

### 3. GET `/api/notes` — Get all notes

**No request body needed.**

**Success response — `200`:**
```json
{
  "success": true,
  "message": "Notes fetched successfully",
  "data": [ ]
}
```

---

### 4. GET `/api/notes/:id` — Get note by ID

**No request body needed.**

**Success response — `200`:**
```json
{
  "success": true,
  "message": "Note fetched successfully",
  "data": { }
}
```

---

### 5. PUT `/api/notes/:id` — Replace a note completely

PUT means **full replacement**. Every field must be provided. Fields not sent will be reset to their default value.

**Request body:**
```json
{
  "title": "Completely new title",
  "content": "Completely new content",
  "category": "personal",
  "isPinned": false
}
```

**Success response — `200`:**
```json
{
  "success": true,
  "message": "Note replaced successfully",
  "data": { }
}
```

> Use `{ new: true, overwrite: true, runValidators: true }` options in your Mongoose call.

---

### 6. PATCH `/api/notes/:id` — Update specific fields only

PATCH means **partial update**. Only send the fields you want to change.

**Request body:**
```json
{
  "isPinned": true
}
```

**Success response — `200`:**
```json
{
  "success": true,
  "message": "Note updated successfully",
  "data": { }
}
```

> Use `{ new: true, runValidators: true }`. Other fields must remain unchanged.

---

### 7. DELETE `/api/notes/:id` — Delete a single note

**No request body needed.**

**Success response — `200`:**
```json
{
  "success": true,
  "message": "Note deleted successfully",
  "data": null
}
```

---

### 8. DELETE `/api/notes/bulk` — Delete multiple notes

**Request body:**
```json
{
  "ids": [
    "64b1f2c3e4d5a6b7c8d9e0f1",
    "64b1f2c3e4d5a6b7c8d9e0f2",
    "64b1f2c3e4d5a6b7c8d9e0f3"
  ]
}
```

**Success response — `200`:**
```json
{
  "success": true,
  "message": "3 notes deleted successfully",
  "data": null
}
```

> Use `Note.deleteMany({ _id: { $in: ids } })`.

---

## PUT vs PATCH — Know the Difference

This is an important concept in REST API design.

| | PUT | PATCH |
|-|-----|-------|
| **Meaning** | Replace the entire document | Update only the fields you send |
| **Fields not sent** | Reset to default | Stay unchanged |
| **Use case** | Full edit form submission | Toggle `isPinned`, change one field |

**Example — you have this note:**
```json
{ "title": "Old title", "content": "Important content", "isPinned": true }
```

**Send PUT with only:**
```json
{ "title": "New title" }
```
**Result — content and isPinned are reset:**
```json
{ "title": "New title", "content": "", "isPinned": false }
```

**Send PATCH with only:**
```json
{ "title": "New title" }
```
**Result — only title changed, rest stays:**
```json
{ "title": "New title", "content": "Important content", "isPinned": true }
```

---

## Response Format

Every endpoint — success or error — must follow this exact format. No exceptions.

```json
{
  "success": true,
  "message": "...",
  "data": { }
}
```

- `success` — always `true` or `false`
- `message` — always a human-readable string
- `data` — object, array, or `null`

---

## HTTP Status Codes

| Code | When to use |
|------|-------------|
| `200` | Successful GET, PUT, PATCH, DELETE |
| `201` | Successful POST — note(s) created |
| `400` | Missing required fields / empty body / invalid ObjectId / empty array |
| `404` | Note not found with that `_id` |
| `500` | Unexpected server or database error |

---

## Validation Rules

- `title` and `content` required on create — return `400` if missing
- On any `/:id` route — validate ObjectId format first, return `400` if invalid
- On any `/:id` route — if note not found, return `404`
- On PATCH — if body is empty, return `400` with `"No fields provided to update"`
- On bulk create — if `notes` array is missing or empty, return `400`
- On bulk delete — if `ids` array is missing or empty, return `400`

---

## Environment Variables

**.env** — never commit this
```
MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/notes-db
PORT=5000
```

**.env.example** — commit this
```
MONGO_URI=your_mongodb_connection_string_here
PORT=5000
```

---

## Packages

| Package | Type | Purpose |
|---------|------|---------|
| `express` | dependency | Web framework |
| `mongoose` | dependency | MongoDB ODM |
| `dotenv` | dependency | Load `.env` variables |
| `nodemon` | devDependency | Auto-restart on save |

```bash
npm install express mongoose dotenv
npm install --save-dev nodemon
```

```json
"scripts": {
  "start": "node src/index.js",
  "dev":   "nodemon src/index.js"
}
```

---


## Submission Process

| # | What to submit |
|---|----------------|
| 1 | **GitHub Repository Link:** Provide the link to your public repository containing the project code. |
| 2 | **Postman Documentation:** Provide a link to your published Postman API documentation. |
| 3 | **Deployed Backend Link:** Provide the live URL where your backend API is deployed. |

---

## Final Note

This assignment covers every HTTP method used in real REST APIs. Once you understand when to use `POST`, `GET`, `PUT`, `PATCH`, and `DELETE` — and the difference between PUT and PATCH — you understand how 90% of backend APIs are designed.


